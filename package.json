{
  "name": "trustless-agentkit",
  "version": "0.1.0",
  "private": true,
  "description": "Trustless AgentKit - Verifiable Compute (zkML) + Verifiable Memory (Kinic) for Coinbase AgentKit",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "example": "examples"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:24804/git/hshadab/coinbase"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "ethers": "^6.15.0"
  }
}
